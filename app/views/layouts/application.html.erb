<!DOCTYPE html>
<html>
<head>
  <title>BackChannel Application</title>
    </head>
  <body>

    <div class="container">
      <header>

        <nav class="round">
          <ul>
            <li><%= link_to "Home", root_path %></li>
            <li><%= link_to "Search", '/search' %></li>
            <% if signed_in? %>
                <li><%= link_to "Profile", current_user %></li>

                 <% if current_user.admin? %>
                    <li><%= link_to "Users", users_path %></li>
                    <li><%= link_to "Create Users/Admins", '/createadmin' %></li>
                    <li><%= link_to "View Reports", '/reports' %></li>
                 <% end %>
                <li><%= link_to "Settings", edit_user_path(current_user) %></li>
                <li><%= link_to "Sign out", '/signout', :method => :delete %></li>
             <% else %>
                <li><%= link_to "Sign in", '/signin' %></li>
                <% end %>

          </ul>
        </nav>
      </header>
      <section class="round">
        <% flash.each do |key, value| %>
          <div class="flash <%= key %>"><%= value %></div>
        <% end %>
        <%= yield %>
      </section>
    </div>
  </body>
</html>
</head>

